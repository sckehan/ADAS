
node {
    /* Requires the Docker Pipeline plugin to be installed */
    docker.image('registry.cn-qingdao.aliyuncs.com/gen4/image-pull-push-test:v1').inside {
        stage('Test') {
            sh 'node --version'
        }
    }
}
