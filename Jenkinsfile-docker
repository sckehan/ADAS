
node {
    /* Requires the Docker Pipeline plugin to be installed */
    docker.image('registry.cn-beijing.aliyuncs.com/gen4/admin-server:latest').inside {
        stage('Test') {
            sh 'node --version'
        }
    }
}
